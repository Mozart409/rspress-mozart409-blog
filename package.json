{
  "name": "rspress-mozart409-blog",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "start": "rspress build && rspress preview"
  },
  "dependencies": {
    "rspress": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.0"
  }
}
