{
  "name": "rspress-doc-template",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "rspress dev",
    "build": "rspress build",
    "preview": "rspress preview",
    "start": "pnpm run build && pnpm run preview"
  },
  "dependencies": {
    "rspress": "^1.6.0"
  },
  "devDependencies": {
    "@types/node": "^20.9.0"
  }
}
